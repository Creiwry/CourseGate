<div class="title">
  <h3>Courses</h3>
</div>
<div class="container">
  <ul>
    <% @courses.each do |course| %>
      <li>
        <%= link_to course.name, course_path(course) %>
      </li>
    <% end %>
  </ul>
</div>
