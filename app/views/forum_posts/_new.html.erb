<%= form_with(model: @forum_post, url: forum_thread_forum_posts_path(@forum_thread), local: true) do |form| %>
<%# TODO: make it work %>
  <%= form.hidden_field "forum_post[forum_thread_id]", value: params[:forum_thread_id] %>
  <%= form.text_area "forum_post[content]" %>
  <%= form.submit "Post" %>
<% end %>
